specVersion: 0.0.1
schema:
  file: ./schema.graphql
dataSources:
  - kind: near
    name: Contract
    network: near-testnet
    source:
      account: "sub-geeta.geetaagrawal.testnet"
    mapping:
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - ExampleEntity
      receiptHandlers:
        - handler: handleReceipt
      file: ./src/mapping.ts
